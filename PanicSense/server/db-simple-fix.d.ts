/**
 * Simple database fix function.
 * Directly creates tables with all the necessary columns.
 * @returns Promise<boolean> True if the fix was applied successfully
 */
export function simpleDbFix(): Promise<boolean>;
